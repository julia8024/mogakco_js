function printNum() {
    num = parseInt(document.getElementById('input_number').value);
    // console.log(num);        // num 값 확인용

    for(i = 0; i <= num; i++) {
        console.log(i);
    }
}